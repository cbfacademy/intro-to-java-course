package com.cbfacademy;

public class questions {

    public static void main(String[] args) {
        // Question 1: is the belo legal? Yes
        // try{
        //     System.out.println( "Hello World!" );
        //     }
        //     finally {
        //         System.out.println("Trying a try and finally block with no catch block!");
        //     }

        // Question 2: Is there anything wrong with the following exception handler as written? Will this code compile?
            // No because exception ArithmeticException has already been caught in Exception line which proceses exception ArithmeticException
        // try {

        // } catch (Exception e) {
        
        // } catch (ArithmeticException a) {
        
        // }

        // Question 4: Whcich of the options below does this code produce?
            // int[] A; 
            // A[0] = 0;
            // [ ] an error
            // [ ] a checked exception e.g., IOException
            // [ ] an unchecked exception: Runtime exceptions: programming errors, such as logic errors or improper use of an API.
            // [ ] a compile error: syntax errors, type mismatches, and other issues that violate the language's rules
            // [ ] no exception
            // Answer: a compile error

        // Question 5: The JVM starts running your program, but the JVM can't find the Java platform classes. (The Java platform classes reside in classes.zip or rt.jar.) What happens (choose 1):
            // [ ] an error
            // [ ] a checked exception
            // [ ] an unchecked exception
            // [ ] a compile error
            // [ ] no exception

            //Answer : NoClassDefFoundError  
    }
   
}
